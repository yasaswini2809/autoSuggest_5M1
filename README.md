# autoSuggest_5M1

# this is my main repo for project
npm init -y (go to packages.json and main:server.js)
create server.js

npm install express
write code in express
create a folder named frontend and a file with